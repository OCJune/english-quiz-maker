# PRD — 자동 영어 문제 제작 프로그램 (가칭 `AriaForge`)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.1 (2026-09-12) — 스파이크 결과 반영 |
| 작성 | 시니어 PM 관점 |
| 기술 스택 | Vite + React + Tailwind CSS + Electron (main/preload는 esbuild — §6.1 참조) |
| 상태 | **P0 + P1 완료** — 생성·조판·문항 편집·프리셋에 PDF/DOCX 내보내기까지. 남은 것은 P2 |
| 구현 | [app/](app/) — 설정·명령·환경 이슈는 [app/README.md](app/README.md) |
| 스파이크 결과 | [spike/SPIKE-REPORT.md](spike/SPIKE-REPORT.md) — S1 11/11, S2 99/99, C1 9/9, C2 9/9, C3 9/9 |

---

## 1. 문제 정의

### 1.1 현재 상태 (As-Is)

내신 영어 변형문제 제작은 현재 **브라우저 도구 + 한글(HWP) 수동 편집**의 2단 파이프라인으로 이루어진다.

```
HWP 지문 열기
  → 본문 복사
  → ariaenglish_offline_4.html 의 Text Area 1 에 붙여넣기
  → 유형 버튼 클릭 (Scramble / Cloze / ... 11종 중 하나)
  → Text Area 3 의 결과 확인
  → [Stack Question & Answer] 클릭 (문제→TA2, 정답→TA4 누적)
  → ★ 위 3단계를 문항 수만큼 반복 (30문항 = 30회) ★
  → [Apply Number] 클릭 (문제/정답 번호 동기화)
  → TA2 / TA4 전체 복사
  → HWP 로 옮겨 2단 레이아웃·폰트·행간 수동 정리
  → 정답지 페이지 분리
  → PDF 내보내기
```

`2026-2 중간 문제/` 의 산출물 14세트(= 약 420문항)가 전부 이 방식으로 제작되었다.

### 1.2 핵심 문제

| # | 문제 | 근거 |
|---|---|---|
| P1 | **반복 클릭 노동** — 문항 1개당 `유형 선택 → 생성 → 스택` 3액션. 30문항 = 최소 90액션 | 매뉴얼 33·36번 기능 설명 |
| P2 | **레이아웃 수작업이 매번 반복** — 2단 배치, 단당 1문항, 제목 블록, 행간 16pt 를 HWP 에서 손으로 맞춤 | 산출 PDF 14세트가 동일 레이아웃 |
| P3 | **유형 구성을 제어할 수 없음** — 기존 도구의 랜덤 버튼은 "11종 중 1개"만 뽑음. "Cloze 3개 + Scramble 2개" 같은 구성 불가 | 매뉴얼 28~31번 |
| P4 | **원하는 출력 변형이 도구에 없음** — 서술형 Scramble(선지 없음), 단어목록 없는 Cloze 는 도구가 지원하지 않아 **생성 후 손으로 지움** | 매뉴얼 9번 vs 산출 PDF Q6 / 매뉴얼 19번 vs 산출 PDF Q7 |
| P5 | **설정 재현 불가** — 난이도·보기 개수·유형 조합이 휘발. 학교별 스타일을 매번 기억해서 재설정 | `Save Temporary`는 메모리 저장, 종료 시 소실 (매뉴얼 35번) |

### 1.3 해결 방향 (To-Be)

검증된 기존 생성 엔진을 **그대로 재사용**하되, 그 위에 **오케스트레이션 + 조판 + 내보내기** 레이어를 올린 데스크톱 앱을 만든다.

```
지문 붙여넣기 + 제목/부제목 입력
  → 유형 포함/제외 체크, 유형별 문항 수(또는 총 개수 랜덤 배분) 지정
  → [생성] 1회 클릭
  → 2단 A4 미리보기에서 검수 (문항 단위 재생성/삭제)
  → PDF / DOCX 내보내기
```

**핵심 가치 제안: 90액션 → 1액션.**

---

## 2. 타겟 사용자

### 2.1 주 사용자 (Primary) — "내신 변형문제를 직접 만드는 영어 강사"

| 속성 | 값 |
|---|---|
| 규모 | 1명 (본인). 로컬 단독 사용 |
| 기술 숙련도 | HWP·브라우저 도구 능숙, 터미널/코드 불필요 |
| 작업 주기 | 학기별 중간·기말 시즌에 집중 (연 4회 피크) |
| 1회 작업량 | 학교×교재×과(Lesson)×섹션 단위로 문서 1개, 문서당 20~30문항. 시즌당 10~15문서 |
| 현재 담당 | 정의여고1, 효문고1, 창동고1, 도선고2, 용화2 등 (교재: 미래엔(김성연), 비상(홍민표), 올림포스) |
| 핵심 니즈 | ① 반복 클릭 제거 ② 기존 PDF와 **동일한 조판** ③ 학교별 설정 프리셋 |
| 성공의 정의 | "지문 붙여넣고 버튼 한 번 누르면 바로 인쇄할 PDF가 나온다" |
| 사용 기기 | **노트북 2대를 옮겨 다니며 작업**, 파일을 주고받음 → 프리셋·세션을 OS 앱데이터 폴더가 아니라 **프로젝트 폴더 기준 상대 경로**에 저장해 폴더째 옮겨도 그대로 열리게 한다 |

### 2.2 2차 수혜자 (Secondary) — 학생

직접 앱을 쓰지 않는다. 산출물(문제지/정답지)의 **가독성과 정답 정확성**으로만 영향을 받는다. 제품 결정 시 "학생이 풀 수 있는 문제인가"가 품질 게이트가 된다.

### 2.3 비(非)사용자 — 명시적 제외

- 다른 강사/학원에 배포할 대상 사용자 **없음** → 로그인·권한·과금·자동 업데이트 불필요
- 학생이 직접 푸는 온라인 모드 **없음** → 채점·성적 기능 불필요

> ⚠️ 배포 계획이 생기는 순간 §10 리스크 R1(서드파티 엔진 재배포 권한)이 **블로커로 승격**된다.

---

## 3. 확정된 제약 조건 및 설계 결정

### 3.1 사용자 확정 사항 (인터뷰 결과)

| # | 결정 | 함의 |
|---|---|---|
| D1 | **생성 엔진**: 원본 `ariaenglish_offline_4.html` 을 숨긴 채 구동 | 문제 생성 알고리즘을 구현하지 않는다. 엔진은 **블랙박스**. 우리 책임은 입출력 계약·파싱·조판 |
| D2 | **입력**: 텍스트 붙여넣기 | `.hwp`/`.hwpx` 파싱 불필요. HWP→클립보드 한 단계는 수동으로 남음 |
| D3 | **출력**: PDF + DOCX | ~~+ HWPX~~ — HWPX 는 구현·구조검증까지 마쳤으나 **사용자 요청으로 제거**(2026-09-12). 한글에서 `.docx` 를 열 수 있어 편집 경로는 DOCX 가 대신한다 |
| D4 | **설정 범위**: 유형별 문항 수 직접 지정 + 프리셋 저장 | 랜덤 배분 모드와 수동 지정 모드 **둘 다** 제공. 프리셋은 디스크 영속 |
| D5 | **지문 범위**: 1개 지문만 (MVP) | 여러 섹션은 문서를 나눠 만든 뒤 사용자가 합침. 다중 지문은 P2 |

### 3.2 기술 조사 결과 — 엔진 통합 계약 (검증 완료)

원본 HTML의 JS 본문은 난독화(`_0x1a2b3c`)되어 있으나, **`onclick`/`onchange` 핸들러와 DOM id 는 난독화되지 않은 안정적 전역 진입점**이다. 이것이 D1을 실현 가능하게 하는 핵심 근거다.

**전역 함수 (호출 가능)**

| 함수 | 역할 |
|---|---|
| `load()` | 초기화. `<body onload>` 로 자동 실행 |
| `generatePerformance(type)` | **주 진입점.** 11개 유형 문자열 중 하나로 문제 1개 생성 → TA3 에 기록 |
| `randomGenerate('Random11')` | 11종 중 랜덤 1개 (사용하지 않음 — 우리가 유형을 직접 통제) |
| `stackContent()` | TA3 → TA2(문제) / TA4(정답) 누적 |
| `applyNumber()` | TA2/TA4 에 `< Question N >` / `< Answer N >` 번호 부여 + 동기화 검증 |
| `setOptionNumber()` / `setOptionDifficulty()` / `setOptionLanguage()` / `setOptionOccupation()` | `<select>` 값을 엔진 내부 상태로 반영 |

**DOM 계약**

| id | Text Area | 역할 | 방향 |
|---|---|---|---|
| `productWordContentOriginal` | TA1 | 원문 지문 | **쓰기** |
| `productWordContentProcessed` | TA3 | 생성된 문제 + 정답 | **읽기** |
| `supportTextArea1` | TA2 | 문제 스택 | 읽기 (선택) |
| `supportTextArea2` | TA4 | 정답 스택 | 읽기 (선택) |

| 설정 `<select>` id | 값 | MVP 정책 |
|---|---|---|
| `productWordOption` | `1`~`5` | 보기 개수. Cloze 에서는 **빈칸 출현 빈도** |
| `productWordDifficulty` | `Hard`/`Normal`/`Easy` | 사용자 노출 |
| `productWordLanguage` | `English`/`Korean`/`Other` | **`English` 고정** |
| `productWordOccupation` | `Teacher`/`Student` | **`Teacher` 고정** (원문·정답 유지) |
| `optionToolConfirm` | `true`/`false` | **`false`(Never Confirm) 강제 고정** ← 자동화 필수 |

**유형 문자열 (11종, 정확한 표기)**

```
Context    : Scramble, Shuffle, Missing Sentence, Wrong Sentence, Correct Sentence
Vocabulary : Multiple Choice, Blank, Binary Word, Wrong Word, Correct Word
General    : Cloze
```

#### 3.2.1 호출 레시피 (S1 실측으로 확정)

| 경로 | 33회 성공률 |
|---|---|
| `window.generatePerformance(type)` 직접 호출 | **0/33** |
| 프라이밍 1회 후 직접 호출 | **1/33** |
| **유형 버튼에 `dispatchEvent(new MouseEvent('click'))`** | **32/33** |
| `webContents.sendInputEvent` (OS 신뢰 입력) | 11/11 (불필요) |

```
1. TA1.value = 정규화된 지문 ; TA3.value = ''
2. 설정 select 값 주입 → 해당 setOption*() 호출
3. document.getElementById(BUTTON[type]).dispatchEvent(
       new MouseEvent('click', { bubbles: true, cancelable: true, view: window }))
4. TA3.value 를 동기적으로 읽음 (폴링 불필요)
5. 결과가 레이블만이면 = 토큰 미무장 → 재시도
   (실패한 시도 자체가 다음 시도를 무장시킨다. 최대 4회)
6. 파싱 → 구조화된 Question 객체 → 후처리 변환
```

**직접 함수 호출은 절대 사용하지 않는다.** OS 수준 신뢰 입력도 필요 없다 — 좌표 계산·창 포커스가 불필요해 구현이 단순해진다. 페이지 로드 후 첫 1회는 항상 버려지는 프라이밍이다.

**유형 → 버튼 id 매핑**

```
Scramble→buttonScramble            Shuffle→buttonShuffle
Missing Sentence→buttonMissingSentence   Wrong Sentence→buttonWrongSentence
Correct Sentence→buttonCorrectSentence   Multiple Choice→buttonMultipleChoice
Blank→buttonBlank                  Binary Word→buttonBinaryWord
Wrong Word→buttonWrongWord         Correct Word→buttonCorrectWord
Cloze→buttonCloze
```

#### 3.2.2 TA3 출력 골격 (S2, 99/99 파싱 확인)

```
(빈 줄) / <유형 레이블> / (빈 줄) / <문제 본문>
[(빈 줄) Choose  =  w  w  ...]    ← Cloze 전용
[(빈 줄) (1) ... ~ (N) ...]       ← 선지 보유 5유형 전용
(빈 줄) / Answer  =  (N)          ← 정식 구분자 (등호 양쪽 공백 2개)
(빈 줄) / <6줄 저작자 푸터>
```

| 유형 | 정답 종류 | 선지 | 선지 형식 |
|---|---|---|---|
| Scramble | choice | ✓ | `(1)  word / word / ...` |
| Shuffle | choice | ✓ | `(1)  (A) - (B) - (D) - (C)` |
| Multiple Choice | choice | ✓ | `(1)  bananas` |
| Blank / Binary Word | choice | ✓ | `(1)  (A) x  (B) y  (C) z` |
| Missing / Wrong / Correct Sentence | choice | ✗ | 본문 `( N )` 마커 |
| Wrong / Correct Word | choice | ✗ | 본문 `( N ) ( word )` 마커 |
| Cloze | **wordList** | ✗ | `1) word` 번호 목록 |

**어떤 유형도 `sentence` 정답을 native 로 내지 않는다** → 서술형 Scramble 은 반드시 후처리 변환이어야 한다 (§6.3 확인).

**조사에서 확인된 주의사항**

- 외부 네트워크 호출(`fetch`/`XMLHttpRequest`/WebSocket), `localStorage`, `eval` **없음** → 오프라인 완전 동작, 샌드박싱 용이
- ✅ **`globalLicense` 는 라이선스 키가 아니라 저작자 크레딧 배너였다** (S1 확인). `getLicense() === globalLicense` → `true`. 잠금 장치가 아니므로 PRD 리스크 R2 는 해소됐다
- ⚠️ **실제 게이트는 일회용 능력 토큰이다** (S1 신규 발견). 모든 생성 함수가 `if (!flagValidate || code !== globalCode) return '';` 로 시작하고, 성공 시 `globalCode` 를 재난수화하며 `flagValidate = false` 로 소진한다. 토큰은 **버튼의 이벤트 경로로 실행될 때만 무장**된다 → §3.2.1 레시피 참조
- ✅ 생성된 모든 출력에 **6줄 저작자 푸터가 항상 부착**된다 (`For Mass Production www.AriaEnglish.com` …). 문항 단위로는 제거하되 **앱 크레딧 화면에 원문 그대로 1회 표시**한다
- 엔진은 오류를 `alert()` 로 표시한다("이 설정과는 별개로 오류 메세지는 항상 출력됩니다" — 매뉴얼 32번) → hidden window 에서 `window.alert`/`window.confirm` 을 **반드시 후킹**해 예외로 변환
- 엔진은 **설정 자동 하향 조정**을 한다고 매뉴얼(5·7번)에 적혀 있다. 단 S2 에서 1,315자 지문으로는 **하향 0건** (요청 1/3/5 → 전달 1/3/5). 짧은 지문에서는 여전히 발생 가능하므로 실제 적용값 표시는 유지한다
- **Cloze 에서 '보기 개수' 는 빈칸 개수를 결정한다** (S2 실측): Hard opt1/3/5 → 1/7/14 빈칸, Easy opt5 → 35 빈칸. 같은 값이 유형마다 의미가 달라지므로 **Cloze 선택 시 라벨을 '빈칸 빈도' 로 전환**한다
- **출력 다양성**: 동일 지문 12회 반복에서 11유형 전부 `distinct 12/12` (중복 0건) → 중복 위험은 우려보다 낮다

### 3.3 기준 레이아웃 스펙 (산출 PDF 실측)

`2026-2 중간 문제/정의여고1_공통영어2_미래엔(김) 1과 본문_1_2.pdf` 실측값:

| 항목 | 측정값 |
|---|---|
| 용지 | A4 595.32 × 841.92 pt (210 × 297 mm) |
| 좌측 단 | x = 28 → 289 pt (폭 261 pt ≈ 92 mm) |
| 우측 단 | x = 309 → 569 pt (폭 260 pt) |
| 단 간격(거터) | 20 pt ≈ 7 mm |
| 여백 | 좌 28 pt / 우 26 pt / 상 25 pt |
| 본문 | 10 pt *(→ 현재 구현은 13px = 9.75pt, 아래 주 참조)* |
| 문항 헤더 (`< Question N >`) | 12 pt *(→ 현재 구현은 본문과 동일한 13px)* |
| 행간 | 16 pt (본문의 1.6배) |
| 페이지당 문항 | **정확히 2개** (30문항 → 15페이지 + 정답 1페이지 = 16페이지) |
| 1페이지 1단 | 제목 블록 + Question 1 (제목이 단을 독점하지 않고 첫 문항이 아래로 흐름) |
| 제목 / 부제목 | `미래엔(김) 1과 본문` / `Section 1, 2` |
| 정답지 | 최종 페이지, `< Answer N >` 순차 목록 |

> **타이포그래피는 실측값에서 의도적으로 벗어나 있다** (2026-09-12 사용자 지정):
> 제목·부제목 **Pretendard 16px bold**, 그 외 전부 **Noto Sans 13px**.
> 페이지·단·행간 수치는 실측 그대로이므로 단당 줄 수와 페이지 수는 변하지 않는다.
> 상세는 [app/README.md](app/README.md#타이포그래피).

**정답 표기 규칙 (실측)**

| 유형 | 정답 형식 | 예 |
|---|---|---|
| 객관식 계열 9종 | 선지 번호 | `< Answer 1 >  (1)` |
| Scramble (서술형) | **정답 문장 원문** | `< Answer 6 >`<br>`Excited for a new experience, we set out to volunteer.` |
| Cloze | 번호 매긴 단어 목록 | `< Answer 7 >`<br>`1) animal`<br>`2) agreed` … |

---

## 4. 사용자 스토리

### Epic A — 지문 입력과 문서 메타데이터

| ID | 스토리 | 인수 조건 |
|---|---|---|
| A1 | 강사로서, HWP 에서 복사한 지문을 텍스트 영역에 붙여넣어 문제 생성의 원본으로 쓰고 싶다 | 붙여넣기 시 탭·다중 공백·non-breaking space(`\xa0`)·스마트 인용부호가 정규화된다. 문자 수와 추정 문장 수가 표시된다 |
| A2 | 강사로서, 문서 제목(`미래엔(김) 1과 본문`)과 부제목(`Section 1, 2`)을 입력해 1페이지 첫 단에 인쇄하고 싶다 | 두 필드 모두 입력 가능하며 미리보기에 즉시 반영된다. 비워두면 제목 블록이 생략되고 Q1 이 단 최상단에서 시작한다 |
| A3 | 강사로서, 지문이 특정 유형의 최소 요건을 못 채우면 **생성 전에** 알고 싶다 | 문장 수 < 4 → Shuffle 불가 경고. 5단어 이상 문장 없음 → Scramble 불가 경고. 해당 유형이 자동으로 비활성 표시된다 |

### Epic B — 유형 선택과 문항 구성

| ID | 스토리 | 인수 조건 |
|---|---|---|
| B1 | 강사로서, 11개 유형을 개별 체크박스로 포함/제외하고 싶다 | 11개 체크박스. `Context 5종` / `Vocabulary 5종` / `General 1종` 그룹 일괄 토글 제공. 전부 해제 시 생성 버튼 비활성 |
| B2 | 강사로서, 총 문항 수만 입력하고 선택 유형에서 랜덤하게 뽑고 싶다 *(랜덤 모드)* | 총 개수 입력 → 선택된 유형에 균등 랜덤 배분. 문항 수 ≥ 유형 수이면 **모든 선택 유형이 최소 1회** 등장 보장 |
| B3 | 강사로서, 유형별 문항 수를 직접 지정하고 싶다 *(수동 모드)* | 유형별 숫자 입력. 합계가 실시간 표시되고, 유형별 생성 가능 상한을 넘으면 경고 |
| B4 | 강사로서, 출제 순서를 정하고 싶다 | `유형별 묶음` / `랜덤 셔플` 중 선택. 기준 PDF 는 혼합 배치 → 기본값 `랜덤 셔플` |
| B5 | 강사로서, 난이도와 보기 개수를 조정하고 싶다 | `Hard/Normal/Easy`, 보기 `1~5`. 엔진이 자동 하향 조정한 경우 문항별로 **실제 적용값**을 배지로 표시 |

### Epic C — 출력 변형 (사용자 요구 4·5번)

| ID | 스토리 | 인수 조건 |
|---|---|---|
| C1 | 강사로서, Scramble 을 **객관식 / 서술형** 중 골라 출제하고 싶다 | **객관식**: `(1)~(5)` 선지 유지, 정답 = 선지 번호. **서술형**: 선지 블록 제거, 본문에 `( 단어 / 단어 / ... )` 만 남고 정답 = **복원된 원문 문장** |
| C2 | 강사로서, Cloze 의 단어 목록을 **표시 / 미표시** 중 골라 출제하고 싶다 | **표시**: `Choose = ...` 줄 유지. **미표시**: 해당 줄 제거. 정답지는 두 경우 모두 `1) word` 번호 목록 |
| C3 | 강사로서, 두 변형 설정이 프리셋에 함께 저장되길 원한다 | 프리셋에 Scramble 모드·Cloze 모드 포함 |

### Epic D — 검수와 조판

| ID | 스토리 | 인수 조건 |
|---|---|---|
| D1 | 강사로서, 인쇄 전 2단 A4 레이아웃을 그대로 미리보고 싶다 | §3.3 실측 스펙대로 렌더. 페이지 단위 스크롤, 페이지 번호 표시 |
| D2 | 강사로서, 마음에 안 드는 문항만 재생성하고 싶다 | 문항별 `재생성`(동일 유형, 다른 결과) / `삭제` / `유형 변경 후 재생성`. 삭제 시 번호가 자동 재부여되고 정답지도 동기화 |
| D3 | 강사로서, 문항 순서를 바꾸고 싶다 | 드래그 또는 위/아래 버튼. 번호·정답지 자동 재동기화 |
| D4 | 강사로서, 문항이 한 단을 넘치면 알고 싶다 | 오버플로우 문항에 경고 배지. 정책: `글자 크기 자동 축소(최소 9pt)` → 그래도 넘치면 `재생성 권장` |

### Epic E — 내보내기

| ID | 스토리 | 인수 조건 |
|---|---|---|
| E1 | 강사로서, 기준 PDF 와 구분되지 않는 PDF 를 받고 싶다 | 단 경계·폰트 크기·행간이 기준 PDF 대비 ±2 pt 이내 |
| E2 | 강사로서, Word 에서 손보고 싶어 DOCX 로 받고 싶다 | 2단 섹션 레이아웃, 문항별 단 분리, 정답지 별 페이지 |
| ~~E3~~ | ~~강사로서, 기존 HWP 워크플로에 맞춰 HWPX 로 받고 싶다~~ | **범위에서 제거.** 한글이 `.docx` 를 열 수 있어 DOCX 가 편집 경로를 대신한다 |
| E4 | 강사로서, 문제지와 정답지를 분리 저장하고 싶다 | `한 파일` / `문제지·정답지 2파일` 선택 |

### Epic F — 프리셋과 세션

| ID | 스토리 | 인수 조건 |
|---|---|---|
| F1 | 강사로서, 학교별 설정 조합을 프리셋으로 저장·불러오고 싶다 | 이름 지정 저장(`정의여고1`, `효문고1`). 유형 선택·유형별 개수·난이도·보기 개수·Scramble/Cloze 모드·정렬 방식 포함. 디스크 영속 |
| F2 | 강사로서, 앱을 닫았다 열어도 작업 중인 문서가 남아있길 원한다 | 지문·설정·생성 결과를 로컬에 자동 저장. 재실행 시 복원 |

---

## 5. 핵심 기능 — MVP vs 이후

### 5.1 P0 — MVP (이것만으로 기존 워크플로를 대체)

| # | 기능 | 비고 |
|---|---|---|
| 1 | ✅ **엔진 브리지**: hidden `BrowserWindow` 에 원본 HTML 로드, `alert`/`confirm` 후킹, `Never Confirm` 강제, `English`/`Teacher` 고정, 직렬 큐, 프라이밍+재시도 | `app/src/main/engine/EngineBridge.ts` — self-test 11/11 |
| 2 | ✅ **TA3 파서**: 생성 결과 → `Question { type, body, options[], answer, warnings[] }` 구조화 | `app/src/main/engine/ResultParser.ts` — 골든 99/99 |
| 3 | 지문 입력 + 정규화 + 제목/부제목 입력 | A1, A2 |
| 4 | 11유형 포함/제외 체크 + 유형별 최소 요건 사전 검증 | B1, A3 |
| 5 | ✅ **랜덤 모드**(총 개수) + **수동 모드**(유형별 개수) | B2, B3. `shared/plan.ts`, 테스트 13건 |
| 6 | ✅ 난이도 / 보기 개수 / 출제 순서 설정 | B4, B5. Cloze 선택 시 '빈칸 빈도'로 라벨 전환 |
| 7 | ✅ **Scramble 객관식↔서술형 전환** (후처리 변환) | C1 — 사용자 요구 4번. `transforms.ts`, 테스트 9/9 |
| 8 | ✅ **Cloze 단어목록 표시↔미표시** (후처리 변환) | C2 — 사용자 요구 5번. `transforms.ts`, 테스트 9/9 |
| 9 | ✅ **Cloze 빈칸 재번호** — 엔진이 읽는 순서와 다른 번호를 부여하는 경우 본문+정답 목록을 함께 재번호 | C3 — S2 신규 발견 (9건 중 3건). `transforms.ts`, 테스트 9/9 |
| 10 | ✅ 중복 문항 감지 + 재시도 (동일 유형이 같은 타겟 문장을 재사용하지 않도록) | 예외 E4. `transforms.ts: fingerprint` |
| 11 | ✅ **2단 A4 미리보기** (§3.3 실측 스펙) | D1 — 사용자 요구 3번. `PreviewPane.tsx` + `shared/print/layout.ts` |
| 12 | ✅ 정답지 생성 (유형별 3가지 정답 형식) | §3.3. 정답지도 2단 — Q2 해소 |
| 13 | ✅ **PDF 내보내기** (`webContents.printToPDF`) | E1. 기준 PDF 대비 단 위치·폰트 일치, 폭 차 0.36pt |
| 14 | ✅ 프리셋 저장/불러오기 + 세션 자동 복원 | F1, F2. `app/.ariaforge/` — 프로젝트 폴더 안이라 노트북 간 이동에 따라옴 |
| 15 | 생성 진행률 + 부분 실패 리포트 | 예외 E5 |
| 16 | ✅ 엔진 self-test (11유형 각 1문항 생성, 실패 시 명확한 오류) | 리스크 R8. `npm run selftest` — 현재 11/11, 문항당 4~26ms |

**MVP 완료 판정:** 지문 붙여넣기 → 제목 입력 → 유형 체크 → 총 30 입력 → [생성] → PDF 저장. 이 경로만으로 `2026-2 중간 문제/` 의 기존 산출물과 **동등한 문서**가 나온다.

### 5.2 P1 — v1.0 (MVP 직후, 동일 릴리스 범위 내)

> D3(PDF+DOCX)은 **확정 스코프**다. P1 으로 둔 것은 축소가 아니라 순서다 — PDF 가 픽셀 기준이므로 레이아웃 엔진을 PDF 로 먼저 검증하고, DOCX 는 **동일 문서 모델의 재렌더링**으로 구현한다.

| # | 기능 | 비고 |
|---|---|---|
| 17 | ✅ **DOCX 내보내기** (2단 섹션 레이아웃) | E2. `DocxExporter.ts`, 테스트 9건 |
| ~~18~~ | ~~**HWPX 내보내기**~~ | **범위에서 제거** — §9.1-16 참조 |
| 19 | 문제지/정답지 분리 저장 | E4 |
| 20 | ✅ 문항 단위 재생성 / 삭제 / 유형 변경 / **본문·선지·정답 직접 수정** | D2. 재생성 시 기존 fingerprint 를 제외해 중복 방지. 수정 내용은 미리보기·PDF·DOCX 에 즉시 반영 |
| 21 | ✅ 문항 순서 재배열 (위/아래 버튼) | D3. 번호·정답지 자동 재동기화, 테스트 17건 |
| 22 | ✅ 단 오버플로우 감지 + 자동 글자 크기 축소 | D4. 미리보기·PDF 공통 fit 패스, 9pt 까지 축소 후 경고 |

### 5.3 P2 — 이후 (가치는 있으나 MVP 가설 검증 후)

| # | 기능 | 미루는 이유 |
|---|---|---|
| 21 | **다중 지문(섹션) 지원** — Section 1/2 를 한 문서에 묶고 지문별 문항 수 배분 | D5 로 1개 지문으로 확정. 실사용 후 불편이 확인되면 착수 |
| 22 | 지문 라이브러리 (학교/교재/과/섹션 트리로 저장·재사용) | 붙여넣기 반복이 실제 병목인지 측정 후 |
| 23 | `.hwpx` 입력 파싱 (붙여넣기 단계 제거) | D2 로 제외. hwpx 는 ZIP+XML 이라 기술적으로 가능 |
| 24 | 학생용/교사용 분리 출력 (`Occupation` 활용) | 현 워크플로에 해당 니즈 미확인 |
| 25 | 문항 품질 스코어링 / 오답 단어 품질 개선 | 엔진이 블랙박스라 개입 지점이 없음. 엔진 교체 시 재검토 |
| 26 | 시드 고정 재현 생성 | 엔진 내부 RNG 제어 불가 — 엔진 교체가 전제 |

---

## 6. 기능 상세 설계

### 6.1 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│ Renderer (Vite + React + Tailwind)                      │
│  ├─ SourcePanel     지문 입력 · 정규화 · 메타데이터      │
│  ├─ ConfigPanel     유형 체크 · 개수 · 난이도 · 변형모드 │
│  ├─ PreviewCanvas   2단 A4 렌더 (CSS columns / 절대배치) │
│  └─ PresetManager   프리셋 CRUD                          │
└────────────────────────┬────────────────────────────────┘
                         │ IPC (contextBridge)
┌────────────────────────┴────────────────────────────────┐
│ Main Process (Electron)                                 │
│  ├─ EngineBridge    hidden BrowserWindow + executeJS    │
│  │                  alert/confirm 후킹, 직렬 큐          │
│  ├─ ResultParser    TA3 텍스트 → Question 객체           │
│  ├─ Transformers    Scramble·Cloze 변형                  │
│  ├─ Composer        문항 배분 · 중복 제거 · 번호 부여     │
│  ├─ Exporters       PDF / DOCX                           │
│  └─ Store           프리셋 · 세션 (JSON on disk)         │
└────────────────────────┬────────────────────────────────┘
                         │ 로컬 파일 로드 (네트워크 없음)
              ariaenglish_offline_4.html  ← 블랙박스 엔진
```

**보안 설정 (엔진 샌드박싱)**

| 설정 | 값 | 이유 |
|---|---|---|
| `nodeIntegration` | `false` | 엔진 페이지에 Node 권한 부여 금지 |
| `contextIsolation` | **`false`** | ⚠️ S1 에서 확정. `preload` 가 페이지 window 를 공유해야 `window.alert`/`confirm` 을 가로챌 수 있다. 이것이 **엔진 파일을 수정하지 않는** 유일한 방법 |
| `preload` | `preload.js` | 다이얼로그 트랩 설치 (페이지 스크립트보다 먼저 실행) |
| `sandbox` | `true` | 엔진은 순수 계산만 수행 |
| `show` | `false` | 사용자에게 보이지 않음. S1 에서 hidden 상태로 11/11 생성 확인 |
| `session` | 전용 파티션 + 모든 외부 요청 차단 | 엔진에 네트워크 코드 없음 (S1: 요청 시도 0건). 심층 방어로 유지 |

**엔진 파일은 수정하지 않는다.** S1 에서 원본 무수정 + preload 조합으로 11/11 생성을 확인했다 (HTML 에 `<script>` 를 주입하는 방식도 동작하지만 파생 사본 관리가 불필요하게 늘어난다).

**빌드 툴체인** — `electron-vite` 는 사용하지 않는다. 이 개발 환경에서 **Vite 프로세스가 2개 이상 동시에 살아 있으면 0xC0000005 로 죽고**, electron-vite 가 정확히 그 패턴에 해당한다 (Node·tsc·vitest·Rollup 단독은 모두 정상이므로 머신 전반의 문제는 아니다). 대신:

| 대상 | 도구 | 이유 |
|---|---|---|
| main / preload | **esbuild** (`scripts/bundle-node.mjs`) | HMR·CSS·JSX 가 필요 없는 코드. 단일 프로세스로 빠르고 안정적 |
| renderer | **Vite** 단독 (`vite.renderer.config.ts`) | React + Tailwind + HMR 이 필요 |

이 구성에서 Vite 는 항상 1개만 돌아 빌드·dev 모두 안정적이다. 사용자가 두 노트북을 옮겨 다니므로 재현 조건과 대처를 [app/README.md](app/README.md) 에 기록해 두었다.

### 6.2 문서 모델 (세 내보내기 포맷의 단일 원천)

```ts
type QuestionType =
  | 'Scramble' | 'Shuffle' | 'Missing Sentence' | 'Wrong Sentence' | 'Correct Sentence'
  | 'Multiple Choice' | 'Blank' | 'Binary Word' | 'Wrong Word' | 'Correct Word'
  | 'Cloze';

interface Answer {
  kind: 'choice' | 'sentence' | 'wordList';
  choice?: number;           // 객관식 9종
  sentence?: string;         // Scramble 서술형
  words?: string[];          // Cloze
}

interface Question {
  id: string;
  number: number;            // 1-based, 재배열/삭제 시 재계산
  type: QuestionType;
  body: string;              // 지시문 + 변형된 본문
  options?: string[];        // 선지 (서술형 Scramble·Cloze 는 없음)
  answer: Answer;
  meta: {
    appliedDifficulty: 'Hard' | 'Normal' | 'Easy';   // 엔진 자동조정 후 실제값
    appliedOptionCount: number;                       // 엔진 자동조정 후 실제값
    scrambleMode?: 'choice' | 'openEnded';
    clozeWordList?: boolean;
    targetFingerprint: string;  // 중복 판정 키
  };
  warnings: string[];
}

interface Document {
  title: string;             // '미래엔(김) 1과 본문'
  subtitle: string;          // 'Section 1, 2'
  sourceText: string;
  questions: Question[];
  layout: LayoutSpec;        // §3.3 실측값이 기본값
}
```

### 6.3 후처리 변환 — 사용자 요구 4·5번 구현

엔진은 두 변형을 **지원하지 않는다**(매뉴얼 9·19번 확인). 따라서 엔진 출력에 변환 레이어를 적용한다. 이것이 현재 수동으로 "생성 후 손으로 지우는" 작업의 자동화다.

#### C1. Scramble → 서술형

| 단계 | 동작 |
|---|---|
| 1 | 엔진이 객관식 Scramble 생성 (본문에 `( 단어 / 단어 / ... )`, 하단에 `(1)~(5)` 선지, 정답 `(2)`) |
| 2 | 정답 선지 번호를 파싱 |
| 3 | 정답 선지의 단어 시퀀스를 `/` 로 분리 → 공백 결합 → **정답 문장** 확보 |
| 4 | **교차 검증**: 복원된 문장이 원문 지문에 실제로 존재하는지 확인. 불일치 시 원문에서 정규화 매칭으로 재시도 |
| 5 | 선지 블록 제거. 본문은 `( 단어 / 단어 / ... )` 상태 유지 |
| 6 | 정답을 `{ kind: 'sentence', sentence }` 로 설정 |
| 실패 | 4단계 실패 → 문항 폐기 후 재생성 (최대 3회), 이후 사용자에게 보고 |

> **S2 실데이터 검증: 9/9 통과.** 정답 선지의 단어 순서를 공백으로 결합한 문장이 9/9 전부 원본 지문에서 verbatim 으로 확인됐고, 본문은 9/9 에서 `( word / word / ... )` 그룹을 유지했다. 기준 PDF Q6 과 동일한 형태다.
>
> ```
> PASS Hard-opt3  words=11  "July 29, Monday Our club arrived at the Free Animals sanctuary."
> PASS Easy-opt3  words=14  "It was amazing to see bears and elephants moving freely in a large field."
> ```

#### C2. Cloze → 단어목록 미표시

| 단계 | 동작 |
|---|---|
| 1 | 엔진이 Cloze 생성 (본문 빈칸 `1) _______________`, 하단 `Choose = word word ...`, 정답 `1) word` 목록) |
| 2 | `미표시` 모드: `Choose = ` 줄을 제거 |
| 3 | 본문·정답 목록은 그대로 유지 |
| 4 | 잔여 힌트 검사 (`Choose` 외 단어 목록 흔적) |

> **S2 실데이터 검증: 9/9 통과.** 단어 목록은 1~35개(보기 개수에 비례), 제거 후 잔여 힌트 0건, 정답 목록 개수가 빈칸 개수와 정확히 일치. 기준 PDF Q7 과 일치.

#### C3. Cloze 빈칸 재번호 (S2 신규 발견 — 원래 PRD 에 없던 필수 단계)

엔진이 빈칸 번호를 **읽는 순서와 다르게** 부여하는 경우가 있다. S2 의 Cloze 9건 중 **3건** 발생:

```
Hard-opt5    [1,2,3,13,4,5,6,7,8,9,10,11,12,14]   ← 13번이 3번과 4번 사이에 등장
Normal-opt3  [1,2,3,4,7,5,6,8,...]
Normal-opt5  [1,2,6,3,4,5,7,...]
```

기준 PDF 의 Cloze(Q7·Q23)는 순서대로였으므로, 현재는 수동 교정하거나 순서대로 나온 것만 골라 쓰고 있을 가능성이 높다. 학생 입장에서 번호가 튀는 Cloze 는 혼란스럽다.

| 단계 | 동작 |
|---|---|
| 1 | 본문에서 `(\d+)\)\s*_+` 를 순서대로 수집 → 등장 순서 배열 |
| 2 | 이미 `1..N` 순서면 변환 없음 |
| 3 | `old → new` 매핑 생성 후 **본문의 빈칸 번호와 정답 목록을 동시에** 재번호 |
| 4 | 재번호 후 본문 순서가 `1..N` 이고 정답 목록이 정렬되었는지 검증 |

> **구현·검증 완료: 9/9 교정 성공** (`spike/parser.js: renumberCloze`).

### 6.4 문항 배분 알고리즘

**랜덤 모드 (B2)** — 총 개수 `N`, 선택 유형 `T`

```
1. N >= |T| 이면: 각 유형에 1개씩 먼저 할당 (모든 유형 최소 1회 보장)
2. 남은 N - |T| 개를 T 에서 균등 랜덤 추출해 분배
3. N <  |T| 이면: T 에서 N 개를 비복원 랜덤 추출
4. 유형별 생성 가능 상한을 초과하는 할당은 다른 유형으로 재분배
5. 출제 순서: '랜덤 셔플'(기본) 또는 '유형별 묶음'
```

**수동 모드 (B3)** — 유형별 개수를 그대로 사용. 상한 초과 시 생성 전 경고.

**중복 제거** — `targetFingerprint` = `유형 + 변형 대상(문장 인덱스/단어 집합)` 해시. 동일 지문에서 같은 지문을 N번 돌리면 엔진이 같은 결과를 낼 수 있으므로, 지문별로 fingerprint 를 기록하고 충돌 시 재생성(문항당 최대 5회 재시도).

### 6.5 생성 실행 모델

- 엔진은 단일 전역 상태(일회용 토큰 포함)를 공유하므로 **직렬 실행 필수** (병렬 호출 금지)
- **페이지 로드 후 첫 생성 1회는 프라이밍으로 버린다** (토큰 무장용)
- 생성은 §3.2.1 레시피(버튼 `dispatchEvent`)로만 수행하고, 레이블만 돌아오면 재시도한다 (최대 4회 — 실패한 시도가 다음 시도를 무장시킨다)
- 문항당 타임아웃 1초 (S1 실측 4~36ms). 초과 시 해당 문항 실패 처리 후 다음으로 진행
- 진행률 `12 / 30 생성 중...` 표시, 취소 가능
- 전체 완료 후 리포트: `성공 28 / 중복 재생성 3 / 실패 2 (Shuffle: 문장 수 부족)`

---

## 7. 성공 지표

### 7.1 핵심 지표 (North Star)

| 지표 | 현재 (추정) | 목표 |
|---|---|---|
| **30문항 문서 1개 제작 소요 시간** | 30~45분 (생성 90액션 + HWP 조판) | **3분 이내** |
| **사용자 액션 수** | ~90+ | **5 이하** (붙여넣기·제목·유형체크·개수·생성) |

### 7.2 품질 지표 (출시 게이트)

| 지표 | 목표 | 측정 방법 |
|---|---|---|
| 무수정 사용 가능 문항 비율 | ≥ 90% | 30문항 × 3세트 육안 검수 |
| 레이아웃 일치도 | 단 경계·폰트·행간 ±2 pt | 생성 PDF vs 기준 PDF 좌표 비교 스크립트 |
| 페이지당 문항 수 | 정확히 2 (예외 0건) | 자동 검증 |
| 정답 정확도 | **100%** | 유형별 골든 케이스 회귀 테스트 |
| 중복 문항 | **0건** | fingerprint 충돌 로그 |
| 생성 실패율 | < 2% | 리포트 집계 |
| 유형 커버리지 (랜덤 모드, N≥유형수) | 선택 유형 100% 등장 | 자동 검증 |
| Scramble 서술형 정답 복원 성공률 | ≥ 98% | C1 4단계 교차검증 통과율 |

### 7.3 운영 지표

| 지표 | 목표 |
|---|---|
| 콜드 스타트 → 생성 가능 상태 | < 3초 |
| 30문항 생성 시간 | **< 5초** (S1 실측: 문항당 4~36ms) |
| PDF 내보내기 | < 5초 |
| 앱 크래시 | 시즌당 0건 |
| 프리셋 재사용률 | 2번째 시즌부터 문서의 80% 이상이 프리셋에서 시작 |

### 7.4 채택 지표 (진짜 성공 신호)

> **다음 시험 시즌에 `ariaenglish_offline_4.html` 을 한 번도 직접 열지 않는다.**

---

## 8. 예외 케이스 (Edge Cases)

### 8.1 입력 관련

| ID | 케이스 | 처리 |
|---|---|---|
| E1 | 지문 문장 수 < 4 | Shuffle 비활성 + 사유 툴팁 (매뉴얼 10번 요건) |
| E2 | 5단어 이상 문장이 없음 | Scramble 비활성 (매뉴얼 9번: "5개 미만 단어 문장은 선별되지 않음") |
| E3 | 지문이 너무 짧아 Cloze 빈칸 요건 미달 | 보기 개수(빈도) 자동 하향 후 실제값 배지 표시 |
| E4 | HWP 에서 붙여넣은 탭·다중 공백·`\xa0`·스마트 인용부호(`’ “ ”`)·전각 문자 | 정규화 파이프라인에서 표준 문자·단일 공백으로 치환 (기준 PDF 추출에서 `\xa0` 실존 확인) |
| E5 | 지문에 한국어 혼재 | `English` 고정이므로 영어 부분만 처리됨(매뉴얼 6번). 한국어 감지 시 "한국어 구간은 문제화되지 않습니다" 안내 |
| E6 | 지문에 날짜·목록 (`July 29, Monday`) 이 문장 경계를 흐림 | 엔진에 위임. 단 결과 검수 대상으로 표시 |
| E7 | 지문에 이미지·표가 있었던 경우 | 텍스트만 전달됨. 범위 외 (§9) |
| E8 | 지문 0자 / 공백만 | 생성 버튼 비활성 |

### 8.2 구성 관련

| ID | 케이스 | 처리 |
|---|---|---|
| E9 | 유형 전부 해제 | 생성 버튼 비활성 + "최소 1개 유형 선택" |
| E10 | 총 문항 수 = 0 또는 음수 | 입력 검증 (1~100) |
| E11 | 수동 모드에서 유형별 합계가 0 | 생성 버튼 비활성 |
| E12 | 요청 문항 수 > 생성 가능한 고유 문항 수 | 가능한 만큼 생성 후 "요청 30 / 생성 24 — 고유 문항 소진" 리포트. 부분 산출물은 정상 사용 가능 |
| E13 | 선택 유형이 지문 요건을 전부 불충족 | 생성 차단 + 불충족 사유 목록 |

### 8.3 엔진 관련

| ID | 케이스 | 처리 |
|---|---|---|
| E14 | 엔진이 `alert()` 로 오류 표시 | `window.alert` 후킹 → 예외로 변환 → 해당 문항 실패 처리, 앱은 멈추지 않음 |
| E15 | `Always Confirm` 이 켜져 `confirm()` 이 자동화를 차단 | 세션 시작 시 `optionToolConfirm = false` 강제 + `window.confirm → () => true` 후킹 (이중 방어) |
| E16 | 엔진이 보기 개수/난이도를 자동 하향 | 요청값과 실제값을 모두 기록, UI 에 실제값 배지 (매뉴얼 5·7번) |
| E17 | TA3 가 완전히 빈 문자열 | 재시도 → 실패 확정 후 리포트 |
| E18 | TA3 포맷이 예상과 다름 (파싱 실패) | 원문 텍스트를 보존한 채 `raw` 문항으로 표시 + 경고. 사용자가 수동 처리 가능 |
| E19 | ~~`globalLicense` 검증 경로가 프로그램적 호출을 거부~~ | ✅ **해소** — 저작자 배너였을 뿐 게이트가 아님 (S1) |
| E35 | 생성 결과가 유형 레이블만 (`
Cloze

`) = 일회용 토큰 미무장 | 정상 동작. 재시도하면 무장된다. 4회 연속 실패 시에만 실패 처리 |
| E36 | Cloze 빈칸 번호가 읽는 순서와 불일치 | C3 재번호 변환 적용 (§6.3). S2 에서 9건 중 3건 발생 |
| E37 | 엔진 파일이 교체·업데이트되어 토큰 메커니즘이나 버튼 id 가 바뀜 | 기동 시 self-test(11유형 각 1문항)로 즉시 감지 + 버전 고정 번들 |
| E20 | 엔진 호출이 1초 내 반환하지 않음 | 타임아웃 → 해당 문항 실패, 다음 진행 |
| E21 | hidden window 가 크래시 | 자동 재생성 + 해당 문항부터 재시도 |

### 8.4 변형 관련

| ID | 케이스 | 처리 |
|---|---|---|
| E22 | Scramble 서술형 — 정답 문장 복원 실패 (원문 교차검증 불일치) | 문항 폐기 후 재생성 (최대 3회). 이후 실패 시 객관식으로 폴백하고 사용자에게 알림 |
| E23 | Scramble 서술형 — 엔진 선지에서 정답 번호 파싱 실패 | E22 와 동일 처리 |
| E24 | Cloze 미표시 — `Choose =` 외 잔여 힌트 존재 | 제거 후 검사. 미검출 잔여물은 경고 배지 |
| E25 | Scramble 대상 문장이 지문 첫 문장/마지막 문장 | 허용 (기준 PDF 에도 존재) |

### 8.5 조판·내보내기 관련

| ID | 케이스 | 처리 |
|---|---|---|
| E26 | 한 문항이 한 단보다 길다 (긴 지문 Cloze 등) | 글자 크기 자동 축소(최소 9pt) → 그래도 초과 시 경고 배지 + 재생성 권장. 2단 점유는 허용하지 않음(단당 1문항 규칙 유지) |
| E27 | 문항 수가 홀수 → 마지막 페이지 한 단이 빔 | 빈 단 유지 (기준 PDF 와 동일). 정답지는 항상 새 페이지에서 시작 |
| E28 | 정답지가 1페이지를 넘음 | 여러 페이지로 자연 흐름. 문제지 마지막 페이지와 섞이지 않음 |
| E29 | 제목/부제목이 길어 2줄 이상 | 제목 블록 높이를 늘리고 Q1 이 아래로 밀림. 단을 넘치면 제목 폰트 축소 |
| E30 | 제목·부제목 미입력 | 제목 블록 생략, Q1 이 단 최상단에서 시작 |
| E31 | 영문 폰트가 시스템에 없음 | 앱에 폰트 번들 (PDF/DOCX 간 일관성 확보) |
| ~~E32~~ | ~~HWPX 가 한글에서 열리지 않음~~ | 해당 없음 — HWPX 범위 제거 |
| E33 | 내보내기 대상 경로가 읽기 전용 / 동일 파일명 존재 | 덮어쓰기 확인 다이얼로그 |
| E34 | 생성 중 앱 강제 종료 | 세션 자동 저장으로 재실행 시 복원 (F2) |

---

## 9. 범위 외 (Out of Scope)

### 9.1 명시적 제외 — 만들지 않는다

| # | 항목 | 이유 |
|---|---|---|
| 1 | **문제 생성 알고리즘 자체 구현** | D1 — 엔진은 블랙박스. 오답 단어 선택 품질, 난이도 판정, 문장 변형 로직에 개입하지 않는다 |
| 2 | **엔진 난독화 해제 / 보호 메커니즘 우회** | 수행하지 않는다. S1 에서 확인된 일회용 토큰은 **정식 버튼 경로로 정상 무장**되므로 우회할 대상이 없다. 저작자 푸터도 앱 크레딧에 원문 표시한다 |
| 3 | **한글 문서(`.hwp` / `.hwpx`) 출력** | `.hwp` 는 CFB + 레코드 구조로 공개 라이브러리가 없고, `.hwpx` 는 §9.1-16 대로 제거됐다. 편집이 필요하면 한글에서 `.docx` 를 연다 |
| 4 | 지문 작성·번역·해설·어휘 리스트 생성 | 별개 제품 영역 |
| 5 | 학생 채점·성적 관리·LMS 연동 | §2.3 |
| 6 | 온라인 서비스·웹 배포·로그인·결제 | 로컬 단독 사용 (§2.1) |
| 7 | 다중 사용자·권한·협업 | 동일 |
| 8 | 자동 업데이트 채널 | 배포 대상 없음 |
| 9 | 한국어/기타 언어 지문 출제 | `English` 고정 (§3.2). 엔진은 지원하나 검증 범위를 늘리지 않는다 |
| 10 | 이미지·표·도표가 포함된 지문 | 텍스트 전용 |
| 11 | 프린터 직접 제어 (용지함·양면 설정) | PDF 산출 후 OS 인쇄에 위임 |
| 12 | 문항 난이도 자동 평가·정답률 예측 | 데이터 없음 |
| 13 | 기존 PDF/HWP 에서 문제 역추출 | 별개 문제 |
| 14 | 엔진의 부가 기능 포팅 (`Extract Vocabulary`, `Shuffle Tool`, `Replace`, Staff 전용 `Password`/`Advanced`) | 현 워크플로에 불필요 |
| 15 | 시드 고정 재현 생성 | 엔진 RNG 제어 불가 (P2-26) |
| 16 | **HWPX 내보내기** | 구현·구조검증(테스트 17건)까지 마쳤으나 2026-09-12 사용자 요청으로 제거. 한글이 `.docx` 를 열 수 있어 편집 경로는 DOCX 가 대신한다. 필요해지면 git 이력에서 복원 가능 |


### 9.2 의도적 보류 — 나중에 할 수 있다

| # | 항목 | 재검토 조건 |
|---|---|---|
| 16 | 다중 지문(섹션) 한 문서 통합 | 실사용에서 문서 합치기가 병목으로 확인될 때 (P2-21) |
| 17 | 지문 라이브러리 | 붙여넣기 반복이 실측 병목일 때 (P2-22) |
| 18 | `.hwpx` 입력 파싱 | 위와 동일 (P2-23) |
| 19 | 학생용/교사용 분리 출력 | 니즈 확인 시 (P2-24) |
| 20 | 엔진 교체 (규칙 기반 자체 구현) | 엔진 품질 불만 또는 재배포 필요가 생길 때. 문서 모델(§6.2)이 이미 엔진 중립적이므로 교체 비용이 낮다 |

---

## 10. 리스크 및 스파이크 결과

### 10.1 리스크

| ID | 리스크 | 영향 | 완화 |
|---|---|---|---|
| ~~**R1**~~ | ~~서드파티 엔진 재배포 권한~~ — 엔진은 AriaEnglish Offline 4 (Engine Design by Dani Sohn, Copyright 2021, 출처: 황인영 영어 카페) 의 저작물 | — | ✅ **해소** — 사용자가 **저작자 허락을 받았다고 확인**(2026-09-12). 배포 패키징 시 허락 범위(대상·기간·조건)를 다시 확인하고, 앱 크레딧 화면의 6줄 저작자 푸터는 계속 표시한다 |
| ~~**R2**~~ | ~~`globalLicense`/`flagValidate` 검증 경로가 프로그램적 호출에서 실패~~ | — | ✅ **해소** (S1). `globalLicense` 는 저작자 크레딧 배너이고 `getLicense()` 와 일치. 게이트가 아니었다 |
| ~~**R3**~~ | ~~TA3 출력 포맷이 불규칙해 파싱이 취약~~ | — | ✅ **해소** (S2). 99개 골든 파일 **99/99 파싱, 경고 0건**. `Answer  =  ` 가 정식 구분자. 골든 코퍼스가 회귀 기준선으로 확보됨 |
| **R9** | **프로젝트 경로의 한글이 `vite build` 를 죽인다** — 이 머신에서 경로에 비ASCII 문자가 포함되면 0xC0000005 로 즉사한다. 동일 디렉터리를 ASCII↔한글 경로로 옮겨가며 3회 재현 (ASCII 3/3 성공, 한글 0/3) | 빌드 불가 | **프로젝트 폴더명을 ASCII 로 변경** (예: `english-quiz-maker`). 설치 중 겪은 pnpm·npm 의 access violation 도 같은 원인으로 보인다. 상세는 [app/README.md](app/README.md) |
| **R8** | **일회용 토큰 메커니즘은 비공개 내부 동작** — 문서화되지 않았고 엔진 버전이 바뀌면 깨질 수 있다 | 앱 동작 중단 | 엔진 파일 **버전 고정 번들** + **기동 시 self-test**(11유형 각 1문항 생성, 실패 시 명확한 오류로 중단). 레시피 근거는 `spike/probe4.js`·`probe5.js` 에 보존 |
| R4 | 엔진 업데이트 시 전역 함수명·DOM id 변경 | 앱 동작 중단 | 엔진 파일을 앱에 **버전 고정 번들**. 기동 시 계약 검증(필수 함수·id 존재 확인) 후 불일치 시 명확한 에러 |
| ~~R5~~ | ~~HWPX 생성 — 한글이 거부하는 XML 구조~~ | — | ✅ **소멸** — HWPX 가 범위에서 제거되어 리스크도 사라졌다 |
| R6 | CSS 2단 렌더가 "단당 정확히 1문항"을 깨뜨림 | E1 미달 | CSS `columns` 자동 흐름에 의존하지 않고 **문항 → 단 슬롯 명시 배치**로 구현 |
| ~~R7~~ | ~~직렬화로 30문항 생성이 느림~~ | — | ✅ **해소** (S1). 문항당 4~36ms → 30문항 1초 미만. 복수 인스턴스 불필요 |

### 10.2 스파이크 결과 (완료)

| ID | 목표 | 결과 |
|---|---|---|
| **S1** | hidden `BrowserWindow` 에서 엔진을 프로그램적으로 호출 | ✅ **통과** — 11/11 유형 생성. 엔진 파일 무수정, 네트워크 요청 0건, 다이얼로그/오류 0건, 문항당 4~36ms. 일회용 토큰 게이트를 발견하고 `dispatchEvent` 레시피로 해결 |
| **S2** | TA3 출력 포맷 전수 수집 및 파서 스펙 확정 | ✅ **통과** — 99개 골든 파일 수집, **99/99 파싱(경고 0)**. C1 9/9, C2 9/9 실데이터 검증. C3(Cloze 재번호) 신규 발견 후 구현·검증 9/9. 보기 개수 불일치 0건, 출력 다양성 11유형 전부 12/12 |

전체 결과는 [spike/SPIKE-REPORT.md](spike/SPIKE-REPORT.md). 재현: `cd spike && npm run s1 && npm run s2 && node verify.js`

**D1(엔진 재사용)은 실현 가능으로 확정**되었다. 대안(규칙 기반 자체 재구현)은 §9.2-20 의 장기 옵션으로만 남는다.

---

## 11. 남은 질문 (구현 중 확인)

| # | 질문 | 현재 가정 | 확인 방법 |
|---|---|---|---|
| ~~Q1~~ | ~~1페이지 1단에서 제목 블록과 Q1 이 같은 단을 공유하는가?~~ | ✅ **해소 — 공유한다.** 구현·검증 완료 (`layout.test.ts`) |
| ~~Q2~~ | ~~정답지는 1단 흐름인가 2단인가?~~ | ✅ **해소 — 2단이다.** 기준 PDF 마지막 페이지의 x0 히스토그램이 28/309에 각 32줄, y는 793pt까지 사용(문항 페이지는 728pt에서 멈춤) |
| Q3 | 기준 PDF 본문 폰트의 실제 이름 (`CIDFont+F2` 로 서브셋 임베딩되어 원래 이름 불명) | 영문 비례 Sans-serif | HWP 원본(`.hwp`)의 서식 확인 |
| ~~Q4~~ | ~~서술형 Scramble 은 별도 프로그램 산출물인가?~~ | ✅ **해소** — 사용자 확인: `어순배열프로그램_201023.zip` 은 이 엔진과 무관. C1 후처리 변환 설계 유지 (S2 에서 9/9 검증) |
| Q5 | 유형별 최대 생성 가능 문항 수의 실제 상한 | 부분 해소 — S2 에서 **12회 반복 전부 고유**(11유형 모두 12/12). 상한은 12보다 크다. 정확한 상한은 실사용에서 측정 |
| Q6 | 기준 PDF 에서 유형 분포에 규칙이 있는가? (샘플: 30문항에 Cloze 2회가 Q7·Q23 으로 균등 분산) | 규칙 없음 — 랜덤 | 14세트 산출물의 유형 시퀀스 집계 |

---

## 부록 A — 기준 산출물 참조

| 항목 | 경로 |
|---|---|
| 엔진 | `ariaenglish/ariaenglish_offline_4.html` (Engineering Sample Engine v0 r4 Fix04 Build0048) |
| 엔진 매뉴얼 | `ariaenglish/ariaenglish 메뉴얼.pdf` (49개 기능 설명) |
| 기준 산출물 | `2026-2 중간 문제/*.pdf` (14세트) / `*.hwp` (편집 원본) |
| 지문 원본 | `2026-2 중간 지문/*.hwp`, `*.hwpx` |
| 레이아웃 실측 기준 | `2026-2 중간 문제/정의여고1_공통영어2_미래엔(김) 1과 본문_1_2.pdf` (30문항 / 16페이지) |
| 관련 도구 | `ariaenglish/어순배열프로그램_201023.zip` (이 엔진과 무관 — 사용자 확인), `어법어휘3종.zip`, `KSAT_220415.exe` |

## 부록 B — 11개 유형 요약 (매뉴얼 기준)

| 유형 | 한글명 | 난이도 영향 | 보기수 영향 | 최소 요건 | 정답 형식 |
|---|---|---|---|---|---|
| Scramble | 단어 배열 | ✗ | ○ | 5단어 이상 문장 1개 | 선지번호 / **문장**(서술형) |
| Shuffle | 문장·문단 배열 | ○ (복잡도) | ○ | **문장 4개 이상** | 선지번호 |
| Missing Sentence | 문장 삽입 | ✗ | ○ | — | 선지번호 |
| Wrong Sentence | 틀린 문장 찾기 | ○ (복잡도) | ○ | — | 선지번호 |
| Correct Sentence | 올바른 문장 찾기 | ○ (복잡도) | ○ | — | 선지번호 |
| Multiple Choice | 빈칸 객관식 | ○ (어휘) | ○ | — | 선지번호 |
| Blank | 빈칸 3개 객관식 | ○ (어휘) | ○ | — | 선지번호 |
| Binary Word | 양자택일 | ○ (어휘) | ○ | — | 선지번호 |
| Wrong Word | 틀리게 쓰인 단어 | ○ (어휘) | ○ | — | 선지번호 |
| Correct Word | 올바르게 쓰인 단어 | ○ (어휘) | ○ | — | 선지번호 |
| Cloze | 문단 빈칸들 | ○ (어휘) | ○ (**빈칸 빈도**) | — | **단어 목록** |
## 부록 C — 스파이크 산출물 (구현 기반)

| 파일 | 프로덕션 대응 | 내용 |
|---|---|---|
| [spike/SPIKE-REPORT.md](spike/SPIKE-REPORT.md) | — | S1·S2 전체 결과 |
| [spike/main.js](spike/main.js) | `EngineBridge` | 엔진 브리지 + 레시피 구현 + S1/S2 하네스 |
| [spike/preload.js](spike/preload.js) | 그대로 사용 | 다이얼로그 트랩 |
| [spike/parser.js](spike/parser.js) | `ResultParser` + `Transformers` | TA3 파서, C1/C2/C3 변환, `FOOTER_LINES` |
| [spike/verify.js](spike/verify.js) | 회귀 테스트 | 골든 코퍼스 전수 검증 |
| `spike/fixtures/passage.txt` | 테스트 픽스처 | 기준 PDF 에서 복원한 지문 (1,315자 / 14문장) |
| `spike/out/golden/**` (99개) | **회귀 기준선** | 11유형 × 난이도 3 × 보기수 3 |
| `spike/out/stack_questions.txt`, `stack_answers.txt` | 조판기 대조군 | 엔진 자체 `stackContent`+`applyNumber` 출력 — 기준 PDF 형식과 동일하고 저작자 푸터가 자동 제거됨 |
| `spike/probe4.js`, `probe5.js` | 근거 보존 | 호출 레시피 도출 과정 (0/33 vs 32/33) |

